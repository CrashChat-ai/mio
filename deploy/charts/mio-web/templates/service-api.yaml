apiVersion: v1
kind: Service
metadata:
  name: {{ include "mio-web.fullname" . }}
  labels:
    {{- include "mio-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mio-web.selectorLabels" . | nindent 4 }}
