apiVersion: v1
kind: Service
metadata:
  name: {{ include "mio-web.api.fullname" . }}
  labels:
    {{- include "mio-web.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mio-web.api.selectorLabels" . | nindent 4 }}
