# env / secrets
.env
.env.*
!.env.example
secrets.env
*.secret
*.key
*.pem

# Go
bin/
dist/
vendor/
*.test
*.out
coverage.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Node (sdk-py docs builds, generated stuff later)
node_modules/

# generated proto (we'll commit the .proto sources, not the gen output, by default)
proto/gen/

# Personal playground / scratch — never committed.
hack/playground/

# local data
appdata/

# channel-pulse prototype: internal channel titles + any real-data snapshots (PHI safety)
prototypes/channel-pulse-stack/backend/channel_names.json
prototypes/channel-pulse-stack/backend/real_*.json

# Go workspace committed — CI relies on it for multi-module resolution.
# Dockerfiles ignore go.work via .dockerignore.

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

.temp
docs/journals

# Local planning artifacts
plans/
.claude/

repomix-output.xml
prototypes